# <project-name> — Home Assistant config

> One-line description of what this config does.

## Files

| File | Purpose |
|---|---|
| `configuration.yaml` | Top-level config — copy relevant sections to your HA config |
| `automations.yaml` | Automations list |
| `scripts.yaml` | Scripts list |

## Installation

1. Copy the relevant YAML blocks into your Home Assistant configuration.
2. Or drop the files into your HA config directory and add `!include` references.
3. Reload / restart Home Assistant.
4. Test each automation using **Developer Tools → Actions** before relying on it.

## Notes

- Entity IDs used: (list them here so it's easy to find what needs updating)
